var express = require('express')
var router = express.Router()

//Add Routing for the Store1 API calls
// router.get('/', async(req, res, next) =>{
//   await fetch(`${req.query.product}`){
//     //Parse JSON response from store API
//     //Organize Information to Response convention and send
//   }
// })

module.exports = router